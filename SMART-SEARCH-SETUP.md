# (deprecated — safe to delete)

Ask My Planner now runs entirely **on-device** in `ask.html` — plain-language
search over your own planner items, no backend, no external services, no setup.

This file and the unused `supabase/functions/planner-smart-search/` folder are
leftovers from an earlier approach and are no longer referenced by anything.
You can delete both whenever convenient.
